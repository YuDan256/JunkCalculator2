// JIT benchmark: closure capturing an upvalue
mk(a) = {
    f = (x) => x + a
    f
}
add = mk(1)
h(n) = {
    s = 0
    i = 0
    while (i < n) {
        s += add(i)
        i += 1
    }
    s
}
print("RESULT " + str(h(2000000)))
