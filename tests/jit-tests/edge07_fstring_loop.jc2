// Edge: f-string interpolation inside a hot loop (callout / possible JIT fallback)
f(n) = {
    s = 0
    i = 0
    while (i < n) {
        t = f"idx={i}"
        s += 1
        i += 1
    }
    s
}
println("RESULT " + str(f(500000)))
