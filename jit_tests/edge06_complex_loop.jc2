// Edge: complex-number arithmetic inside a hot loop (likely unsupported by JIT)
f(n) = {
    z = 1 + 2i
    s = 0
    i = 0
    while (i < n) {
        w = z * z
        s += 1
        i += 1
    }
    s
}
println("RESULT " + str(f(1000000)))
