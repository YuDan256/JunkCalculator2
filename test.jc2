macro time_it(body) = {
    return quote {
        local a = clock()
	    $body
	    local b = clock()
	    println(f"Time: {b - a}")
    }
}

x = sym("x")
@time_it {
    println(f"Result: {integ(x^4*sin(x)*exp(x), x)}")
}
